# Enabled by default: HTTP Basic Auth for /admin
# The admin UI location in nginx.conf includes this file.
# Credentials are stored in /etc/nginx/.htpasswd-boxion (created at container start if missing).

auth_basic "Restricted";
auth_basic_user_file /etc/nginx/.htpasswd-boxion;
